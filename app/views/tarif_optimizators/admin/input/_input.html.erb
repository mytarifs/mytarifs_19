<%

content_in :tabs do |user| 
  [
    [
     'Выбор параметров расчета',
     (
	  render(:partial => 'tarif_optimizators/admin/input/calculation_choices', :layout => 'application/layouts/filtr_form_n_columns', 
	    :locals => {:form_item => calculation_choices} )
		
     )          
    ], 

    ([
     'Выбор тарифа и набора опций для расчета',
	  render(:partial => 'tarif_optimizators/admin/input/services_for_calculation_select', :layout => 'application/layouts/filtr_form_n_columns', 
	    :locals => {:form_item => services_for_calculation_select} )
    ] if show_services_for_calculation_select_tab), 

    ([
     'Optimization params',
     (
	  render(:partial => 'tarif_optimizators/admin/input/optimization_params', :layout => 'application/layouts/filtr_form_n_columns', 
	    :locals => {:form_item => optimization_params} )
		
     )          
    ] if current_user_admin?), 

    ([
     'Service choices',
     (
	  render(:partial => 'tarif_optimizators/admin/input/admin_service_choices', :layout => 'application/layouts/filtr_form_n_columns', 
	    :locals => {:form_item => service_choices} )
		
     )          
    ] if current_user_admin?), 

    ([
     'Services list',
      render(:partial => 'tarif_optimizators/admin/input/services_select', :layout => 'application/layouts/filtr_form_n_columns', 
        :locals => {:form_item => services_select} )
    ] if current_user_admin?), 

  ].compact 
end
%>
