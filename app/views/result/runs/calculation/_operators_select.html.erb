<%
form_item.init do |f|
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control", 
		:action_name => result_detailed_calculations_optimization_options_path(hash_with_region_and_privacy({:id => params[:id]}))}
	label_html = {:class => "control-label"}
    s = session_filtr_params(f)
	
  [
  	[
     label(filtr_name, "tel", label_html) do
     	image_tag('tele2.png', alt: "Теле2") + 
 	 	check_box(filtr_name, "tel", (s["tel"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false )
 	 end, "",

     label(filtr_name, "bln", label_html) do
     	image_tag('beeline.png', alt: "Билайн") + 
 	 	check_box(filtr_name, "bln", (s["bln"] == 'true' ? {:checked => 'checked', :class => "sm"} : {} ).merge(input_html), true, false )
 	 end, "",

     label(filtr_name, "mgf", label_html) do
     	image_tag('megafon.png', alt: "Мегафон") +
 	 	check_box(filtr_name, "mgf", (s["mgf"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false )
 	 end, "",

     label(filtr_name, "mts", label_html) do
     	image_tag('mts.png', alt: "МТС") + 
 	 	check_box(filtr_name, "mts", (s["mts"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false )
 	 end, "",
  	],

  ]
	    
  end
end
%>
