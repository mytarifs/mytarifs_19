<% title "Выбор способа детального подбора тарифа #{region_and_privacy_tag}" %>
<% description "Выбор способа детального подбора тарифа #{region_and_privacy_tag}. www.mytarifs.ru — сервис по подбору тарифов и опций для \
				мобильных операторов МТС, «Билайн», «Мегафон», TELE2. Помогаем принять правильное решение! Предлагаем широкий \
				выбор средств для анализа услуг в области мобильной связи." %>
<% keywords "выбор способа детального подбора тарифа  #{region_and_privacy_tag}" %>	
<% content_for :hide_title, 'true' %>
<% noindex 'googlebot' %>

<%= render :partial => 'result/runs/calculation/steps', :locals => {:current_step => 1} %>

<header id="first">
    <div class="header-content header-content-smaller">
        <div class="inner">
            <h1 class="cursive"><%= title %></h1>
            <hr />
        </div>
    </div>
</header>

<p>
	В настоящее время возможны 3 варианта подбора тарифа:
	<ul>
		<li>"Все операторы" - это базовый вариант, при котором подбор происходит среди всех операторов и тарифов.</li>
		<li>Вы можете проверить какие будут месячные затраты для выбранного тарифа и опций с помощь варианта "Выбранный тариф".</li>
		<li>Также можете подобрать тариф только для выбранного оператора с помощью варианта "Выбранный оператор".</li>
	</ul> 
</p>

<p>
	<%= render( :partial => 'result/runs/calculation/optimization_select_filtr', :layout => 'application/layouts/filtr_form_inline', :locals => {:form_item => optimization_select_filtr}) %>
</p>

<p>
	<%= link_to("Вернуться к выбору детализации", result_detailed_calculations_select_calls_path(hash_with_region_and_privacy({:id => params[:id]})), 
		{:class => "btn btn-primary btn_text_always_on_screen", :type =>"button", :remote => false, :rel => "nofollow"}) %>
	
</p>

<%= render :partial => 'calculation_info', :locals => {:result_run => result_run} %>
