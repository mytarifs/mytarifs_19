<%
form_item.init do |f|
#  f.caption = "Choice of service"	
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", 
		:action_name => result_service_sets_results_path(hash_with_region_and_privacy)}
	label_html = {} #{:class => "control-label "}
    s = session_filtr_params(f)
#	s = session[:filtr][filtr_name]
	
  [
  	[
#	label(filtr_name, "result_run_id", "Выбор вариант подбора тарифа", label_html),
    '',
	collection_select(filtr_name, "result_run_id", Result::Run.where(:user_id => current_or_guest_user_id), :id, :full_name, 
	  	   {:include_blank => false, :selected => s["result_run_id"] }, input_html )
  	],
  ]
	    
  end
end
%>
