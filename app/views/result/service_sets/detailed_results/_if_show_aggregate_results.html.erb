<%
form_item.init do |f|
#  f.caption = "Choice of service"	
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", 
		:action_name => result_service_sets_detailed_results_path(
			hash_with_region_and_privacy({:result_run_id => params[:result_run_id], :service_set_id => controller.service_set_id}))}
	label_html = {:class => "control-label "}
    s = session_filtr_params(f)
	
  [
  	[
     label(filtr_name, "if_show", "Показать агрегированные значения по мобильным услугам?", label_html),
 	 check_box(filtr_name, "if_show", ((s["if_show"].blank? or s["if_show"] == 'true') ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
# 	 select(filtr_name, "if_show_aggregate_customer_results", [["show", true], ["not show", false]], {:selected => s["if_show_aggregate_customer_results"]}, input_html ),
  	],
  ]
	    
  end
end
%>
