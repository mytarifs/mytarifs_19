<%
form_item.init do |f|
#  f.caption = "Choice of service"	
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", 
		:action_name => result_compare_path(hash_with_region_and_privacy({:result_run_id => params[:result_run_id]}))}
	label_html = {:class => "control-label "}
    s = session_filtr_params(f)
	
  [
  	[
     label(filtr_name, "rouming", "Роуминг", label_html),
 	 check_box(filtr_name, "rouming", ((s["rouming"].blank? or s["rouming"] == 'true') ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
  	],

  	[
     label(filtr_name, "service", "Услуга", label_html),
 	 check_box(filtr_name, "service", (s["service"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
  	],

  	[
     label(filtr_name, "direction", "Направление связи", label_html),
 	 check_box(filtr_name, "direction", (s["direction"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
  	],

  	[
     label(filtr_name, "geo", "Куда", label_html),
 	 check_box(filtr_name, "geo", (s["geo"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
  	],

  	[
     label(filtr_name, "operator", "На какого оператора", label_html),
 	 check_box(filtr_name, "operator", (s["operator"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
  	],	
  ]
	    
  end
end
%>
