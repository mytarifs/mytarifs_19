<%
form_item.init do |fi|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", :action_name => action_name}
	label_html = {:class => "control-label "}
	s = session_filtr_params(fi) || {}
  
  fi.set_fields do |filtr_name|
    [
      [
      	label(filtr_name, "support_service_id", "Тарифы и опции, с которыми применяется опция #{tarif_class.name}", label_html),
      	collection_select(filtr_name, "support_service_id", services_with_support_service, :id, :name, 
	  	{:include_blank => false , :selected => s["support_service_id"].try(:to_i) || services_with_support_service[0].try(:id) }, input_html ),

	  ],
    ] 
  end
end
%>
