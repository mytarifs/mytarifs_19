<%
table.init do |t|
  t.caption = 'Listing users'
  t.heads = ['', 'Name', '', '', '']
  t.set_fields do |user| 
  [
    user.id, 
    user.name, 
    link_to( 'Show',user_path(user), ), 
    link_to( 'Edit', edit_user_path(user)), 
    link_to( 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' })
  ] 
  end
end
%>
