<%
form_item.init do |fi|
  input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", :action_name => scrap_tarif_lists_path(hash_with_region_and_privacy)}
  label_html = {:class => "control-label "}
  s = session_filtr_params(fi)	

  fi.set_fields do |filtr_name|
    [
	   [
      	label(filtr_name, "show_choice", "show_choice", label_html),
     	select(filtr_name, "show_choice", ['body', 'service_desc', 'row_body', 'h1', 'error_404', 'fixed_month_payment', 'fixed_day_payment'], 
	  	  {:include_blank => false, :selected => s["show_choice"]}, input_html.merge({:multiple => false, :size => 7})),
	   ],
    ].compact
  end
end
%>
