<%
form_item.init do |f|
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", :action_name => action_name}
	label_html = {} #{:class => "control-label "}
    s = session_filtr_params(f)        
	
  [
  	[
  	label(filtr_name, "clean_cache", "clean_cache", label_html),
  	select(filtr_name, "clean_cache", ['true', 'false'], 
  	{:selected => (s["clean_cache"] || 'false')}, input_html.merge({:size => 2}) ),

  	label(filtr_name, "show_only_command_keys", "show_only_command_keys", label_html),
  	select(filtr_name, "show_only_command_keys", ['true', 'false'], 
  	{:selected => (s["show_only_command_keys"] || 'false')}, input_html.merge({:size => 2}) ),

  	label(filtr_name, "calculate_on_background", "calculate_on_background", label_html),
  	select(filtr_name, "calculate_on_background", ['true', 'false'], 
  	{:selected => (s["calculate_on_background"] || 'false')}, input_html.merge({:size => 2}) ),
  	],

  ]
	    
  end
end
%>
