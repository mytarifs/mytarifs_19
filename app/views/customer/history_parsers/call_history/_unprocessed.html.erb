<%
table.init do |t|
#  t.caption = 'Unprocessed call history'
#  t.base_name = 'unprocessed_call_history'
#  t.current_id_name = 'unprocessed_column'
#  t.pagination_per_page = 10
#  t.id_name = 'unprocessed_column'
  model_keys = t.model.collect {|row| row.keys }.flatten.uniq.collect{|key| key unless [].include?(key)}.compact  
  t.heads = model_keys.collect{|model_key| model_key.to_s.split('_').join(' ')}
  t.set_fields do |f| 
  	model_keys.collect do |key|
  	  if key == 'price_value'
  	  	f[key].round(2) if f[key]
  	  else
  	  	f[key]
  	  end 
    end
  end
end
%>
