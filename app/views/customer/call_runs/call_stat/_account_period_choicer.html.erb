<%
form_item.init do |f|
#  f.caption = "Choice of service"	
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", 
		:action_name => customer_call_stat_path(hash_with_region_and_privacy({:id => params[:id]}))}
	label_html = {:class => "control-label "}
    s = session_filtr_params(f)
  [
  	[
     label(filtr_name, "accounting_period", "Календарный период", label_html),
     select(filtr_name, "accounting_period", (account_period_options ? account_period_options.map{|a| [a, a]} : []), 
	  	   {:selected => s["accounting_period"] }, input_html )
  	],
  ]
	    
  end
end
%>
