<% title "Список описаний моделирования, детализации звонков" %>
<% description "Список описаний моделирования, детализации звонков. www.mytarifs.ru — сервис по подбору тарифов и опций \
				для мобильных операторов МТС, «Билайн», «Мегафон», TELE2. Помогаем принять правильное решение! Предлагаем \
				широкий выбор средств для анализа услуг в области мобильной связи." %>
<% keywords "список описаний моделирования детализации звонков" %>	
<% noindex 'googlebot' %>

<p>
<%= link_to( 'Создать новое описание', new_customer_call_run_path(hash_with_region_and_privacy), 
	{:class => "btn btn-success", :type => 'button', :rel => "nofollow"})  if controller.is_allowed_new_call_run? %>
</p>

<p>
<%= render(:partial => 'customer/call_runs/admin/call_runs_select', :layout => 'application/layouts/filtr_form_n_columns', :locals => {:form_item => call_runs_select} ) if user_type == :admin %>
</p>

<%= render :partial => 'call_runs', :layout => 'application/layouts/model_table', :locals => {:table => controller.customer_call_runs}%>

