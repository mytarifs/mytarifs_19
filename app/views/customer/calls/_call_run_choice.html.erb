<%
  	controller.call_run_choice
  	filtr_name = 'call_run_choice_filtr'
    s = session[:filtr][filtr_name] || {}
	input_html = {:filtr_name => filtr_name, :class => "updatable form-control ", 
		:action_name => customer_calls_set_calls_generation_params_path(hash_with_region_and_privacy)}
   	label_html = {:class => "control-label "}
%>

<%= label(filtr_name, "customer_call_run_id", "Выбор описания моделирования", label_html) %>

<%= collection_select(filtr_name, "customer_call_run_id", Customer::CallRun.where(:user_id => current_or_guest_user_id), :id, :full_name, 
  	   {:selected => s["customer_call_run_id"] }, input_html ) %>

